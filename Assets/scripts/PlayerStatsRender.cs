using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * TODO: render the player's stats in app and keep it updated as they complete quets
 */

public class PlayerStatsRender : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
