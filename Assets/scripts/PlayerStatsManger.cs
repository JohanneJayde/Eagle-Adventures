using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * TODO: Player stats need to be check in order to render them and make sure that the user
 * isn't able to redo quest that they have completed and that a quest should be unlocked
 */

public class PlayerStatsManger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
